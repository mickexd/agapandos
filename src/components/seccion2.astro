---
// No props needed for this component
---

<section id="nosotros" class="h-screen bg-[#6c6464] relative overflow-hidden">
  <!-- Mobile background image with blur -->
  <div class="lg:hidden absolute inset-0 z-0">
    <img
      src="/images/seccion2.jpg"
      alt="Seccion 2 Background"
      class="w-full h-full object-cover filter blur-xs"
    />
    <div class="absolute inset-0 bg-black/30"></div>
  </div>

  <div class="flex flex-col lg:flex-row w-full h-full relative z-10">
    <!-- Desktop image - hidden on mobile -->
    <div class="hidden lg:flex w-1/2 justify-end pb-24">
      <img
        id="seccion2-image"
        src="/images/seccion2.jpg"
        alt="Seccion 2 Image"
        class="h-[85%] w-auto rounded-xl self-end"
      />
    </div>
    <div
      class="w-full lg:w-1/2 flex flex-col justify-end text-white px-4 lg:px-12 pt-8 lg:py-0 overflow-hidden"
    >
      <div class="shadow-md">
        <h1
          id="seccion2-h1"
          class="backdrop-blur bg-black/20 text-white text-4xl md:text-5xl xl:text-4xl 2xl:text-6xl font- text-center mb-4 lg:mb-0 lg:absolute md:top-1/5 xl:top-1/5 lg:left-1/2 lg:transform lg:-translate-x-1/2 lg:-translate-y-1/2 z-10 lg:w-auto lg:py-2 lg:px-4 shadow-letter italianno-regular text-shadow-md rounded-xl"
        >
          Un lugar para descansar
        </h1>
      </div>

      <div class="flex-1 overflow-y-auto lg:flex lg:flex-col lg:justify-end">
        <p
          id="seccion2-p"
          class="text-xs md:text-[12px] lg:text-[12px] xl:text-[16px] 3xl:text-[12px] self-start pb-4 lg:pb-24 px-1 sm:px-2 lg:px-8 leading-relaxed shadow-lg lg:shadow-none text-left lg:overflow-y-scroll lg:h-3/4"
        >
          Cabañas Los Agapandos representa un concepto arquitectónico innovador
          que combina sostenibilidad ambiental con el máximo confort. Pioneros
          en turismo ecológico responsable, nuestras instalaciones aprovechan
          inteligentemente los recursos naturales, especialmente la energía
          solar, sin comprometer las comodidades que esperas de un alojamiento
          de calidad. <br /><br /> Ubicadas en un entorno rústico y preservado, nuestras
          cabañas han sido diseñadas como un santuario donde podrás desconectarte
          del estrés urbano y reconectarte contigo mismo. Cada espacio está cuidadosamente
          pensado para ofrecerte momentos de relajación genuina y renovación personal.
          <br /><br /> Hemos aprovechado y respetado las bondades de los recursos
          naturales, principalmente el sol, sin descuidar las comodidades de un alojamiento
          normal, lo que hacen de nuestros espacios un lugar ideoneo para el relajamiento,
          reconección y reposo del bullicio y estres propio de la ciudad. <br />
          <br />
          <span class="font-bold">Para los adultos:</span> Disfruta de áreas exclusivas
          perfectas para reuniones íntimas con amigos, cenas al aire libre y momentos
          de convivencia con una copa de vino bajo las estrellas. <br /><span
            class="font-bold">Para los más pequeños:</span
          > Más de 3,000 metros cuadrados de jardines seguros donde los niños pueden
          explorar, jugar y crear recuerdos inolvidables en contacto directo con
          la naturaleza. <br />
          <span class="font-bold">Para toda la familia:</span> Amplias terrazas que
          invitan a la convivencia, ideales para compartir historias, contemplar
          el paisaje y crear vínculos duraderos.
        </p>
      </div>
    </div>
  </div>

  <script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    function initScrollAnimations() {
      const section = document.querySelector("#nosotros");
      const h1 = document.getElementById("seccion2-h1");
      const p = document.getElementById("seccion2-p");
      const img = document.getElementById("seccion2-image");

      if (section && h1 && p && img) {
        // Set initial states
        gsap.set([h1, p, img], { opacity: 0 });
        gsap.set(h1, { y: 100 });
        gsap.set(p, { y: 50 });

        // Create timeline for coordinated animations
        const tl = gsap.timeline({
          scrollTrigger: {
            trigger: section,
            start: "top 80%",
            toggleActions: "play reverse play reverse",
          },
        });

        // Add animations to timeline
        tl.to(img, {
          opacity: 1,
          duration: 0.8,
          ease: "power2.out",
        })
          .to(
            h1,
            {
              y: 0,
              opacity: 1,
              duration: 1,
              ease: "power3.out",
            },
            "-=0.4"
          ) // Start 0.4 seconds before image finishes
          .to(
            p,
            {
              y: 0,
              opacity: 1,
              duration: 1,
              ease: "power3.out",
            },
            "-=0.6"
          ); // Start 0.6 seconds before h1 finishes
      }
    }

    // Initialize animations when DOM is ready
    document.addEventListener("DOMContentLoaded", () => {
      initScrollAnimations();
    });
  </script>

  <style>
    /* Custom scrollbar styling for the paragraph */
    #seccion2-p::-webkit-scrollbar {
      width: 8px;
    }

    #seccion2-p::-webkit-scrollbar-track {
      background: rgba(108, 100, 100, 0.2);
      border-radius: 4px;
    }

    #seccion2-p::-webkit-scrollbar-thumb {
      background: rgba(255, 255, 255, 0.3);
      border-radius: 4px;
      transition: background 0.3s ease;
    }

    #seccion2-p::-webkit-scrollbar-thumb:hover {
      background: rgba(255, 255, 255, 0.5);
    }

    /* Firefox scrollbar styling */
    #seccion2-p {
      scrollbar-width: thin;
      scrollbar-color: rgba(255, 255, 255, 0.3) rgba(108, 100, 100, 0.2);
    }
  </style>
</section>
